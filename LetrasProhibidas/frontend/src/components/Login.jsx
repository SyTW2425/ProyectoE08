import { Background } from "./Background"

export const Login = () => {
  return(
    <Background>
      <div className="h-[40rem] w-[60rem] border-[10px] rounded-xl border-white/10 backdrop-blur-xl">
        Esto es el login
      </div>
    </Background>
  )
}